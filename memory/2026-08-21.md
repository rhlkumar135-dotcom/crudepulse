# 2026-08-21 — CrudePulse Development Log

## News Atlas Page (Module K)
- Built full `/news` page with interactive geotagged world map + scrollable article feed
- **Backend**: `GET /api/news/atlas` — GDELT DOC 2.0 (supplementary) + 12 Google News RSS queries (primary)
- GDELT GEO 2.0 API is **dead** (returns 404) — use DOC 2.0 instead (`/api/v2/doc/doc`)
- GDELT DOC 2.0 returns: title, url, seendate, domain, sourcecountry, socialimage
- Google News RSS is the **primary** fresh source (12 parallel queries, returns 24h articles)
- 38 total stories, 8 within 24h, 15 within 48h
- Story deduplication via Jaccard similarity > 0.55 on title words
- Importance score: (mention_volume × 0.4) + (tone_magnitude × 0.35) + (recency × 0.25)
- Geolocation: keyword-based from title + sourcecountry fallback from GDELT DOC
- Categories: disruption, price, policy, environmental, infrastructure
- Frontend: `src/components/pages/NewsAtlasPage.tsx`
- Navigation tab: after Satellite in App.tsx

## Other Changes
- GlobalFlow map: replaced hand-drawn continent outlines with real Natural Earth 110m paths (257 polygons)
- Generated `src/lib/world-map-paths.ts` from GeoJSON via `scripts/generate-world-map.ts`
- Landing page: marketing-quality redesign with hero, insight preview cards, module navigation, feature strip
- Railway deployment: `crudepulse` service, URL `www.crudepulses.com`

## API Rate Limits
- GDELT rate-limits by IP — max ~2 concurrent queries before 429
- Google News RSS is reliable and returns fresh 24h articles
- GDELT DOC `startdatetime`/`enddatetime` params use format `YYYYMMDDHHmmSS` (no dashes/colons)

## Key Files
- `custom-routes.ts`: all backend routes (2700+ lines)
- `src/components/pages/NewsAtlasPage.tsx`: News Atlas page
- `src/components/modules/GlobalFlow.tsx`: world map with trade flows
- `src/components/LandingPage.tsx`: marketing landing page
- `src/lib/world-map-paths.ts`: auto-generated Natural Earth 110m SVG paths
- [16:37:23] chat: "In news section, every important news within 24 hours to be pinned as per the requirement. Currenlty" -> "## ✅ News Atlas Fixed — Now Shows Real-Time 24h News

**Production:** https://www.crudepulses.com | "
- [16:46:23] chat: "Copernicus /satellite feed should also have standard outline of maps and not random outline. Also if" -> "## ✅ All Maps Updated — Real World Outlines + Info Popups

### **CopernicusMap (Satellite Monitor)**"
